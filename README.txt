main.java
	Uses import_movies.txt to updates red dwarf database. 
	This version should run without arguments.

db_helper.java
	Basic CRUD functionality. Most usage is documented within the program itself.
	May need to be run with java -cp .:postgres.jar <file location> db_helper